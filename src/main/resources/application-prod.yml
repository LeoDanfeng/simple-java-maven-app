server:
  port: 9000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${mysql_url:jdbc:mysql://localhost:3308/test?serverTimezone=Asia/Shanghai}
    username: ${mysql_user:developer}
    password: ${mysql_passwd:DevPasswd123.}
  redis:
    host: ${redis_host:localhost}
    port: ${redis_port:6379}
    database: ${redis_db:0}
